# WinstyleS 技术设计文档

> 本文档记录项目的详细技术设计，供开发者参考。

## 目录

1. [功能规格](#功能规格)
2. [扫描维度](#扫描维度)
3. [数据结构](#数据结构)
4. [CLI 接口](#cli-接口)

---

## 功能规格

### 核心功能

```
WinstyleS 功能模块
│
├── 🔍 扫描与审计 (Scan & Audit)
│   ├── 系统外观扫描
│   ├── 终端配置扫描
│   ├── 开发工具扫描
│   └── 默认值对比分析
│
├── 📤 导出 (Export)
│   ├── 配置文件收集
│   ├── 资源文件提取
│   ├── 选择性导出
│   └── 打包压缩
│
├── 📥 导入 (Import)
│   ├── 包完整性校验
│   ├── 预览变更清单
│   ├── 依赖自动安装
│   └── 配置应用
│
└── 🛠️ 工具 (Utilities)
    ├── 创建系统还原点
    ├── 回滚到上次状态
    └── 导出包检视器
```

### 设计原则

| 原则 | 说明 |
|------|------|
| 🔍 透明 | 所有操作前先预览，用户确认后再执行 |
| 🛡️ 安全 | 修改系统前自动创建还原点 |
| 📦 完整 | 导出包包含所有资源文件，不仅是路径 |
| 🔌 可扩展 | 模块化插件设计 |
| 🤖 自动化友好 | CLI 优先，所有功能可脚本化 |

---

## 扫描维度

### 系统外观 (System Appearance)

| 扫描项 | 数据位置 | 提取内容 |
|--------|----------|----------|
| 系统字体替换 | `HKLM\...\FontSubstitutes` | 注册表 + .ttf/.otf |
| 字体链接 | `HKLM\...\FontLink\SystemLink` | 注册表 |
| 桌面壁纸 | `%AppData%\...\TranscodedWallpaper` | 图片文件 |
| 主题色/强调色 | `HKCU\...\Themes\Personalize` | 注册表 |
| 深色/浅色模式 | 同上 | 注册表 |
| 鼠标指针 | `HKCU\Control Panel\Cursors` | 注册表 + .cur/.ani |

### 终端配置 (Terminal)

| 终端 | 配置位置 | 提取内容 |
|------|----------|----------|
| Windows Terminal | `%LocalAppData%\Packages\...\settings.json` | JSON + 背景图 |
| PowerShell Profile | `$PROFILE` | .ps1 文件 |
| Oh My Posh | `~\.config\ohmyposh\` | .omp.json |

### 开发工具 (Dev Tools)

| 工具 | 配置位置 | 提取内容 |
|------|----------|----------|
| VS Code | `%AppData%\Code\User\settings.json` | 字体/主题相关字段 |
| JetBrains | `%AppData%\JetBrains\...\options\` | XML 配置 |

---

## 数据结构

### 导出包结构

```
MyStyle_20250127/
├── manifest.json           # 包元数据
├── checksums.sha256        # 完整性校验
├── configs/
│   ├── registry/           # .reg 文件
│   ├── terminal/           # 终端配置
│   └── vscode/             # 编辑器配置
├── assets/
│   ├── fonts/              # 字体文件
│   ├── wallpapers/         # 壁纸
│   └── cursors/            # 鼠标指针
└── scripts/                # 安装脚本
```

### manifest.json 关键字段

```json
{
  "version": "1.0.0",
  "created_at": "2025-01-27T12:00:00+08:00",
  "source_system": {
    "os": "Windows 11",
    "version": "23H2",
    "build": "22631"
  },
  "contents": {
    "fonts": [...],
    "registry": [...],
    "terminal": {...}
  },
  "import_instructions": {
    "requires_admin": true,
    "requires_restart": false
  }
}
```

---

## CLI 接口

### 命令总览

```bash
winstyles scan          # 扫描系统配置
winstyles export        # 导出配置包
winstyles import        # 导入配置包
winstyles diff          # 对比两个包
winstyles inspect       # 检视包内容
winstyles restore       # 回滚
```

### 常用示例

```bash
# 扫描所有配置
winstyles scan

# 仅扫描字体和终端，输出 JSON
winstyles scan -c fonts -c terminal -f json

# 导出配置包
winstyles export ./my-style.zip

# 预览导入（不实际应用）
winstyles import ./my-style.zip --dry-run

# 导入并跳过还原点
winstyles import ./my-style.zip --skip-restore-point
```

---

## 相关文档

- [README.md](../README.md) - 项目概述
- [CONTRIBUTING.md](../CONTRIBUTING.md) - 贡献指南和架构说明
